> Historical research snapshot from dc3df23, retained to preserve prior work. Not current guidance. Corrections and current status are in the parent research directory.

# THETA GitHub Gap Matrix

Cross-references THETA's own subsystems against the external corpus
inspected in `GITHUB_REPO_RESEARCH_LEDGER.md`. "Owner" follows the
directive's split: Codex leads broker/execution/persistence/scheduler/
copy-engine; Claude leads strategy/quant/risk/sizing/routing/backtesting-
methodology. Rows with no inspected reference repo yet are marked
honestly rather than filled with a guess.

**2026-09-11 update:** the R6 (expectancy calibration) blocker rows below
now have dedicated companion specs — `THETA_EV_MODEL_SPEC.md` (target
variable definition, empirical-readiness status), `THETA_WALK_FORWARD_SPEC.md`
(fold structure, grouping, stratified reporting), and
`THETA_FEATURE_TARGET_MATRIX.md` (X/Y inventory) — written this session in
direct response to the "attack the real remaining quant blocker" directive.
`lambdaclass/options_portfolio_backtester`'s `engine/engine.py` and
`convexity/scoring.py` were also read this session (see the ledger's
updated entry) to inform those specs.

| THETA subsystem | Current implementation | Reference repos (inspected) | Best external method found | Current deficiency | Proposed improvement | Validation required | Owner | Integration status |
|---|---|---|---|---|---|---|---|---|
| Universe selection | `universe-discovery.ts`, `universe-policy.ts` — real staged Alpaca discovery (assets → bars → optionability) | none inspected yet | — | none identified against inspected corpus | none proposed | — | Claude | N/A |
| Option-chain filtering | `option-chain-ingestion.ts`, hard-gate funnel in `universe-policy.ts` | `alpacahq/options-wheel` (`filter_options`) | delta/yield/OI band filtering (same shape THETA already has) | none — THETA's funnel already reports DEFERRED vs REJECTED per gate, which `options-wheel`'s flat filter list does not | none proposed | — | Claude | N/A |
| IV | Optionomics-sourced only; THETA does not compute IV itself | `thedhruvhegde/ivsurf` (partial), `FlashAlpha-lab/gex-explained` (rough IV approx, explicitly flagged unfit for production by its own authors) | ivsurf's T→0 guard pattern | unconfirmed whether THETA's Optionomics-consumption path has an equivalent T=0 guard | write a regression test confirming DTE=0 Greeks/IV never crash/divide-by-zero | a THETA unit test, not a code change (no adoption needed — Optionomics owns IV compute) | Claude | NOT_STARTED (test not yet written) |
| IV rank/percentile | not found in current codebase search this pass | none inspected yet | — | unknown whether this exists in THETA at all | audit `bots/theta/quant/` for an existing IV-rank implementation before assuming a gap | a code search, not yet performed | Claude | UNKNOWN — needs a codebase audit first |
| Skew | not found in current codebase search this pass | `thedhruvhegde/ivsurf` (not yet read beyond black_scholes.py) | — | unknown | read ivsurf's surface/skew files in a follow-up pass | — | Claude | NOT_STARTED |
| Term structure | Cboe research layer has `NearTermVolRatio` (VIX/VIX9D) — a narrow, single-pair term-structure proxy | `thedhruvhegde/ivsurf` (not yet read beyond black_scholes.py) | — | THETA's only term-structure signal is the VIX-family ratio, research-only | follow-up read of ivsurf's interpolation/surface files before proposing anything | — | Claude | Existing Cboe layer unchanged this pass |
| GEX | none — THETA has no GEX signal at all | `FlashAlpha-lab/gex-explained` (full read) | BSM-gamma-based per-strike GEX, gamma-flip, call/put wall, explicit dealer-sign-convention documentation | total absence of any dealer-positioning signal | `ADAPT`: independently reimplement the GEX formula as a NEW, isolated, research-only module (mirroring `cboe-regime.ts`'s own "research-only until ablation" discipline exactly) | cross-check against a second independent GEX implementation (e.g. `zrack/gex-terminal`, not yet read) before adoption; OOS ablation before any router/AEGIS wiring | Claude | NOT_STARTED — no code written |
| Volatility regime | `regime_v0.py`/`regime-contract.ts` (existing, real, wired into new-risk pipeline) | `FlashAlpha-lab/gex-explained`'s POSITIVE/NEGATIVE GEX regime heuristic | N/A — THETA's regime classification is already broader (RV/MA-slope/gap-based) than a single GEX-sign heuristic | none | none proposed | — | Claude | N/A |
| Entry logic | `theta_q_baseline.py`/`theta_q_lattice.py` (real, tested) | `alpacahq/options-wheel` (`filter_options`/`score_options`) | none better found — THETA's hard-veto + ownership + Pareto-frontier approach is already more rigorous than options-wheel's single-score filter | none | none proposed | — | Claude | N/A |
| Strike selection | delta-band lattice in `latticeConfig` | `alpacahq/options-wheel` (delta band filter) | equivalent method, nothing better found | none | none proposed | — | Claude | N/A |
| DTE selection | `minDte`/`maxDte` lattice config | `alpacahq/options-wheel` (annualized-yield DTE weighting) | none better found | none | none proposed | — | Claude | N/A |
| Expected value | `ev_net` — honestly `None` (no calibrated entry-outcome model yet, an acknowledged, explicit gap); target variable now formally specified as `Y = WholeChainPnl/CapitalDays` in `THETA_EV_MODEL_SPEC.md`, status explicitly `EV_MODEL_NOT_EMPIRICALLY_READY` | `ksanjay/Kelly-Criterion-Option-Selector` (uses `N(d2)` as a fake win probability — a documented ANTI-pattern) | none — no inspected repo has a real calibrated EV model; the Kelly repo is useful only as a negative example | missing historical point-in-time options-chain data to fit/validate against (named datasets/sample-size gap in `THETA_EV_MODEL_SPEC.md` §1) | none proposed beyond the spec itself — the correct fix is real historical data + a real calibrated model (R6), not a delta-derived proxy | full walk-forward per `THETA_WALK_FORWARD_SPEC.md`, plus `MODEL_REGISTRY.md`'s existing promotion criteria | Claude | Target/status spec written this session; model itself NOT_STARTED (blocked on data) |
| Tail risk | `expected_tail_loss`/`p_severe_drawdown` fields exist in `pareto_frontier.py`/`severe_drawdown_spec.py` | `lambdaclass/options_portfolio_backtester` (`convexity/scoring.py`, read this session) | a Rust-backed daily convexity-ratio scorer for deep-OTM tail-hedge puts — a DIFFERENT strategy family (buying protection) from THETA's premium-selling design, not directly reusable | confirmed: no direct adoption path exists from this specific repo; THETA's tail-risk dimensions remain as-is | none proposed from this repo; THETA's existing `expected_tail_loss`/`p_severe_drawdown` fields stand unchanged | — | Claude | Follow-up read COMPLETE this session; `REFERENCE_ONLY`, no integration |
| Assignment model | `assignment_model.py` + `assignment_contract.py`/`assignment-orchestrator.ts` (real, tested, K1) | `alpacahq/options-wheel` (execution.py/state_manager.py not yet read) | unknown — Codex's category, not deep-read by Claude this pass | unknown | Codex to inspect `options-wheel`'s assignment handling directly | — | Codex | Codex-owned, not assessed by Claude |
| Ownership quality | `ownership_v0.py`/`ownership-contract.ts` (real, tested) | none inspected with an ownership-quality equivalent | — | none identified | none proposed | — | Claude | N/A |
| Position sizing | `sizing.py`/`sizing_contract.py` — risk-budget/collateral/concentration qty caps | `HasibVortex369/riskkit` (full read), `ksanjay/Kelly-Criterion-Option-Selector` (negative example) | riskkit's notional-cap-with-backward-consistent-risk% pattern | none, confirmed by test — THETA's design derives `capital_required` from the final capped quantity directly, never a separate fractional representation, so the class of bug riskkit's pattern guards against cannot occur | none needed — verified already-correct by construction | `bots/theta/tests/quant/test_sizing.py::CapConsistencyTests` (2 new tests, both passing) | Claude | VERIFIED — no change needed |
| Kelly-style sizing | not used directly — THETA uses caps, never a raw Kelly formula | `HasibVortex369/riskkit` (half-Kelly-only usage — matches THETA's own discipline), `ksanjay/...` (raw full-Kelly — rejected) | half-Kelly-as-a-ceiling-only pattern, never the primary sizer | none — THETA already avoids raw Kelly entirely, consistent with both the standing instruction and riskkit's own convention | none proposed (THETA's current avoidance of Kelly-as-primary-sizer is already the safer choice both repos converge on) | — | Claude | N/A |
| Portfolio concentration | `tickerConcentrationPct`/`portfolioCapitalAtRiskPct` in `account-exposure.ts`, AEGIS caps | none inspected with an equivalent yet | — | none identified against inspected corpus | none proposed | — | Claude | N/A |
| Correlation | not found in current THETA codebase (documented gap in `aegis-derivation.ts`'s own docstring: "no real price-return-correlation baseline exists yet") | `HasibVortex369/riskkit` (`src/riskkit/correlation.py`, not yet read this pass) | unknown — follow-up read required | THETA has an acknowledged gap; riskkit may have a usable, license-safe (MIT) correlation-limit pattern | read `correlation.py` in a follow-up pass before proposing anything | — | Claude | NOT_STARTED (follow-up read pending) |
| CSP management | `management_action_value.py` + `management_contract.py`/`management-orchestrator.ts` (real, tested, K1) | `alpacahq/options-wheel` (not yet read for management logic) | unknown | unknown | Codex/Claude joint follow-up read of `options-wheel`'s roll/close logic | — | Claude (management economics) + Codex (execution of any roll) | Existing THETA implementation unchanged |
| Rolls | `RollCandidate`/`RollUtility` already implemented in `management_action_value.py` | none deep-read with roll-specific logic yet | — | none identified against inspected corpus | none proposed | — | Claude | N/A |
| Assignment | see "Assignment model" row above | see above | see above | see above | see above | — | Codex (execution) / Claude (economics) | Existing THETA assignment-acceptance economics unchanged |
| Recovery | `recovery_decision.py`/`recovery_contract.py`/`recovery-orchestrator.ts` (real, tested, K2); `SELL_PARTIAL_STOCK` explicitly `{modeled: false}`, no model exists anywhere | none inspected has a partial-liquidation model either | — | THETA's own gap (`SELL_PARTIAL_STOCK` unmodeled) remains genuinely unfilled by this pass's corpus | search a wider recovery/tail-hedge corpus (e.g. `lambdaclass`'s convexity module) once read | — | Claude | Gap remains, per explicit instruction not to invent a model |
| Covered calls | `covered_call_ranker.py` + `covered-call-contract.ts`/`covered-call-orchestrator.ts` (real, tested, K2/K3) | `alpacahq/options-wheel` (single-score filter, inferior) | none better found | none — THETA's WAIT-by-default, multi-factor CC ranker is already more rigorous | none proposed | — | Claude | N/A |
| Execution | Non-executing throughout (by design, this branch) | `alpacahq/options-wheel` (`core/execution.py`/`core/broker_client.py`, not yet read), `milgar7969/alpaca-options-framework` (documented "API workarounds," not yet read) | unknown — Codex's category | unknown | Codex to inspect both execution files directly | — | Codex | Codex-owned, not assessed by Claude |
| Fill modeling | none in THETA (no live order submission on this branch) | `goldspanlabs/optopsy` (4 named slippage models: mid/spread/liquidity/per-leg) | the liquidity-scaled fill-ratio formula, and the "extra leg = extra slippage" per-leg model | THETA's `execution-quality-contract.ts` models spread/fill-probability/quote-size but has not been checked against optopsy's per-leg-count slippage scaling specifically | check whether THETA's execution-quality model already accounts for multi-leg fill degradation (relevant once THETA-DEFINED-RISK spreads are built) | compare `execution-quality-contract.ts`'s formula against optopsy's `per_leg` model (method comparison only — AGPL-3.0 forbids source reuse) | Claude (quant model) / Codex (real execution) | NOT_STARTED |
| Slippage | `estimatedSlippagePerContract` (a flat, caller-supplied constant in `costAssumptions`) | `goldspanlabs/optopsy` (liquidity- and leg-count-scaled slippage, not a flat constant) | volume-scaled slippage model | THETA's slippage assumption is currently a flat constant, not scaled by liquidity or leg count | consider a liquidity-scaled slippage model once THETA-DEFINED-RISK (multi-leg) exists; premature for the current single-leg-only CSP/CC paths | an ablation comparing flat vs. liquidity-scaled slippage against realized fills, once paper fills exist | Claude (model) / Codex (real fills) | NOT_STARTED — flagged, not urgent while THETA remains single-leg |
| Broker reconciliation | `order-intent-state.ts`, `ledger-contract.ts` (existing, tested) | `alpacahq/options-wheel` (not yet read for reconciliation logic) | unknown | unknown | Codex to inspect directly | — | Codex | Codex-owned, not assessed by Claude |
| Scheduler | `scheduler.ts`/`scheduler-engine.ts` (real lease/heartbeat/priority engine, R1I, not deployed) | none inspected has an equivalent yet (LEAN is the relevant Tier-0 reference, not yet inspected this pass) | — | LEAN's scheduling architecture not yet reviewed | read LEAN's algorithm-manager/time-slice scheduling as an architecture reference before further scheduler work | — | Codex (production scheduler) / Claude (engine semantics, already built) | Existing THETA scheduler-engine unchanged; LEAN review pending |
| Persistence | `persistence-repositories.ts` (interfaces only, item M) | none inspected has an equivalent yet | — | none assessed this pass | Codex to compare against LEAN's/optopsy's own persistence patterns if useful | — | Codex | Codex-owned, not assessed by Claude |
| Copy trading | `copy.*` schema, follower/master engine (Codex-owned, `migrations/005`) | none of the inspected 15 repos address copy trading at all | — | none assessed — outside this pass's corpus | none proposed | — | Codex | Not addressed by this research pass |
| Backtesting | none — R6 not started | `goldspanlabs/optopsy` (AGPL-3.0, method-only), `lambdaclass/options_portfolio_backtester` (MIT, `engine.py` now read) | optopsy's four-slippage-model + calendar-spread-separate-path pattern; lambdaclass's composed `BacktestEngine` (data/strategy/execution/portfolio/risk/analytics) and its `HedgeFillWarning` point-in-time-chain-availability discipline | THETA has no backtest engine at all | when R6 begins, independently reimplement (never copy from optopsy given its license) the multi-leg join + named-slippage-model architecture and the composed-engine shape; add a `HedgeFillWarning`-equivalent reason code to `universe-discovery.ts` once historical chain replay exists | full look-ahead/survivorship/execution-model review of both repos before any method is trusted, per the directive's own 9-point checklist | Claude | Architecture reference reading COMPLETE this session (`engine.py`); actual engine build NOT_STARTED (R6 not begun) |
| Walk-forward | none — R6 not started; fold structure/grouping/stratified-reporting now fully specified in `THETA_WALK_FORWARD_SPEC.md` | `lambdaclass/options_portfolio_backtester` (`engine/clock.py`/`pipeline.py`/`multi_strategy.py` still not read) | lambdaclass's clock-driven (not one-shot-vectorized) time-stepping design is structurally more look-ahead-resistant, per `engine.py`'s own composition — not yet confirmed by reading `clock.py` itself | unknown until `clock.py`/`pipeline.py` are read | read `clock.py`/`pipeline.py` before treating the look-ahead-resistance claim as confirmed | — | Claude | Spec written this session; `lambdaclass`'s time-stepping internals still NOT_STARTED |
| OOS validation | none — R6 not started; protocol now specified in `THETA_WALK_FORWARD_SPEC.md` (builds on `DATASET_AND_LABEL_CONTRACT.md` §5, already frozen) | none of the inspected repos demonstrate a real OOS/walk-forward split this pass | — | unknown | follow-up read of `lambdaclass`'s `clock.py`/`pipeline.py` before R6 | — | Claude | Spec written this session; execution NOT_STARTED |
| Performance attribution | none — R6 not started | `lambdaclass/options_portfolio_backtester` (`analytics/tearsheet.py`, `analytics/stats.py`, not yet read) | unknown | unknown until read | follow-up read before R6 | — | Claude | NOT_STARTED |

## Summary of concrete follow-ups this matrix generates

1. ~~Write a THETA unit test confirming `sizing.py`'s reported
   `capital_required`/quantity stays internally consistent after any qty
   cap binds (riskkit comparison finding).~~ **DONE this pass** —
   `CapConsistencyTests` in `test_sizing.py`; confirmed THETA's design is
   already immune to this bug class by construction (no fix needed).
2. Write a THETA regression test confirming DTE=0 / T=0 handling never
   divides by zero anywhere Greeks/IV are consumed (ivsurf comparison
   finding).
3. Follow-up file-level read of `lambdaclass/options_portfolio_backtester`'s
   `engine/`, `convexity/`, and `analytics/` directories before R6
   architecture begins.
4. Follow-up file-level read of `HasibVortex369/riskkit`'s
   `correlation.py` and `drawdown.py` against THETA's own documented
   correlation gap.
5. If GEX is ever adopted, cross-check `FlashAlpha-lab/gex-explained`'s
   formula against a second independent implementation (candidate:
   `zrack/gex-terminal`, not yet read) before writing any THETA code.
6. LEAN's scheduling/portfolio-accounting architecture has not yet been
   reviewed this pass (Tier-0 reference, requested by the directive) —
   flagged for a dedicated follow-up given its likely relevance to both
   the scheduler (Codex) and lifecycle accounting (shared) rows.
