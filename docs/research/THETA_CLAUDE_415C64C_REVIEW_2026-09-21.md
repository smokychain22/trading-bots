# Claude A-D repair review at `415c64c`

Reviewed the commit-level diff and the source and tests for the four research families against canonical `7c19abe`. This is an engineering disposition, not an empirical validation. No Claude branch merge or cherry-pick was made. The production TypeScript policy and Alpaca execution paths remain authoritative.

| Family | Files reviewed | Repair assessment | Disposition |
| --- | --- | --- | --- |
| A, event PIT | `src/research/event-pit-toolkit.ts`, `tests/event-pit-toolkit.test.ts` | An existing first-observed identity is immutable in the fold. Historical provider timestamps require an explicit independent-verification attestation. The canonical database event-revision writer still owns production observation. | `RESEARCH_ONLY`. Accept the repair pattern, but do not promote a research PIT verdict without real corroborating data. |
| C, correlation | `src/research/correlation-cluster-research.ts`, `tests/correlation-cluster-research.test.ts` | Same-symbol exposure is summed, and unknown pairwise correlations are distinct from observed near-zero values. | `RESEARCH_ONLY`. No AEGIS authority until real coverage and conservative treatment of missing pairs are proven. |
| D, IV and spread | `src/research/historical-iv-spread-feasibility.ts`, `src/research/iv-spread-real-data-runner.ts`, their tests | One observation must jointly establish identity, timestamp and historical retrievability. Sessions are sorted and capped at a completed-session cutoff. Optionomics quote evidence is explicitly named historical research data rather than executable broker BBO. | `RESEARCH_ONLY`. The accepted Optionomics adapter can supply historical observations when a real, PIT-safe export is available. |
| B, severe downside | `bots/theta/quant/research/severe_drawdown_continuous_target.py`, `severe_drawdown_cohort_quantile_baseline.py`, `severe_drawdown_real_data_runner.py`, `bots/theta/quant/calibration/severe_drawdown_logistic_baseline.py`, their tests | Forward observations start after decision date. Labels have an availability date. Walk-forward training purges labels that mature at or after the test decision. | `RESEARCH_ONLY`. No trained model, threshold or profitability conclusion was supplied by this commit. |

All four changes repair defects identified in the earlier acceptance review, but their tests use constructed inputs. A canonical sanitized export with real session coverage, immutable source hashes and label-availability semantics is still required before any result can be called empirical. The commit is not needed by the production event-coverage or Pareto-safety correction, so it remains on Claude's isolated branch. None of these packages authorizes an order or replaces the canonical policy provider.
