# Claude A-D review against canonical `853beb4`

Review source: `origin/claude/theta-management-challenger` at `c1de4bd`. This is a file-level engineering review, not a model-validation or provider-capability receipt. No branch was merged or cherry-picked. All four packages have `brokerAuthority=false` in this review. The remaining branch-only research files and docs stay on Claude's branch; they are not implicitly accepted by this A-D review.

| Commit | Source inspected | Disposition | Required repair before integration |
| --- | --- | --- | --- |
| `3e381a8` A, event PIT | `src/research/event-pit-toolkit.ts`, `tests/event-pit-toolkit.test.ts` | REPAIR_AND_PORT, research only | The minimum-of-observations fold can backdate an already persisted first observation if later replay input supplies an earlier time. The `PIT_SAFE_PROVIDER_TIMESTAMP` verdict trusts a provider-known timestamp without proof of historical availability. Keep the canonical event normalizer and add an immutable first-observed store before using this toolkit for decisions. |
| `ba16cd8` C, correlation clusters | `src/research/correlation-cluster-research.ts`, `tests/correlation-cluster-research.test.ts` | REPAIR_AND_PORT, research only | `new Map(positions.map(...))` replaces earlier same-symbol positions instead of summing exposure. Missing pairwise correlations must be distinguished from observed low correlation. No AEGIS concentration authority until real coverage and aggregation tests pass. |
| `33550cc` D, historical IV/spread | `src/research/historical-iv-spread-feasibility.ts`, `tests/historical-iv-spread-feasibility.test.ts` | REPAIR_AND_PORT, research only | Separate `.some()` checks can combine schema evidence from two inadequate endpoint observations. A field name and generic timestamp do not establish historical PIT availability. Shock and stress calculations trust caller ordering, so enforce cutoff/observation ordering and separate same-day data before any backfill or model use. |
| `1d3902c` B, severe downside | `bots/theta/quant/research/severe_drawdown_continuous_target.py`, `severe_drawdown_cohort_quantile_baseline.py`, `severe_drawdown_tail_recommendation.py`, `bots/theta/quant/calibration/severe_drawdown_logistic_baseline.py`, their four tests | DEFER pending real-data/PIT repair | Decision-date ordering is not enough when prior labels have not matured. Same-day adjusted close may postdate an intraday decision. Add outcome-availability, grouped splits and purge/embargo before empirical claims. No trained model or production threshold was verified. |

The canonical `src/theta/normalized-event-evidence.ts` now treats a reused provider event ID with a changed payload as a conflict. This is a narrow safety repair independent of Claude A. `src/theta/universe-discovery.ts` now derives contract-search dates from its injected decision clock, not the host wall clock. Neither change qualifies negative event coverage or enables new Paper entry.

Provider limitation: [Alpaca's corporate-actions reference](https://docs.alpaca.markets/us/reference/corporateactions-1) says records may arrive after announcement and `data_quality=complete` excludes unprocessed incomplete records. A successful empty response cannot certify absence. [Optionomics' public calendar guide](https://docs.optionomics.ai/features/calendar/) describes the product UI, not complete authenticated API coverage. `universe.unsupportedCorporateActionPending` and `universe.eventNear` therefore remain UNKNOWN in canonical discovery.
