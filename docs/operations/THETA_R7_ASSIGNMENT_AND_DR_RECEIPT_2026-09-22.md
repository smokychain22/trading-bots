# R7 assignment integration and recovery checkpoint, 2026-09-22

This is a source and infrastructure receipt, not a Paper-entry authorization.

- Canonical main after the source and board changes: `6a359a088c8375cbe2f0403c3f324348a4966350`.
- Management assignment capacity no longer depends on the unpopulated fusion `riskState.assignmentCapacity`. The management input derives whole-contract capacity from the fresh Alpaca account snapshot, exact short-put contract, GOOD broker reconciliation, matching option position, open ledger lots, strike and multiplier. It counts already-secured lots separately from free options buying power. Missing or stale required evidence remains UNKNOWN. An absent broker-confirmed short put blocks prospective `ACCEPT_ASSIGNMENT`. Broker-confirmed assignment remains lifecycle truth regardless of this prospective action frontier.
- Management decision evidence now freezes `decisionAsOf` after required persisted observations have been read. Evidence observed after that instant blocks active/structural action selection. A future target-quote producer must fetch and persist target quotes before this freeze.
- Focused management tests, full Node suite, Python suite, typecheck, lint, build and security scan passed. The management query passed read-only `EXPLAIN` on Aiven. [CI run 35716139674](https://github.com/smokychain22/trading-bots/actions/runs/35716139674) passed browser checks, migrations and disposable-PostgreSQL tests for the source commit `e64d554b74a69c294d500cb1e3d99987ed436872`.
- The fresh local Aiven backup `2026-09-22_100128-c600e016` contains a custom PostgreSQL archive, schema dump, object/global inventories, all-table row counts, sequence state, critical digests, external assets, checksums and Git bundle. Its source database was 687,044,287 bytes and had 153 application tables. The isolated real restore passed structure fingerprint parity (`9421fe659efd8ea4dbf8648b690436e6b14058015f9f11a70c93064934718dc2`), row-count parity and critical-data verification. The previous verified backup was preserved. Local path: `C:\ProjectBackups\trading-bots\daily\2026-09-22_100128-c600e016`.
- Aiven still reports `default_transaction_read_only=on`. The supported capacity or larger-target remedy has not been approved or performed. No production database mutation, migration, worker restart, broker mutation or Paper order was made in this checkpoint.
- The existing worker remains at `853beb4fde989c2f6deb83ad9cb13a9a3e87e76a`, mode `MASTER_THETA_PAPER`, state `DEGRADED`, execution gate `LOCKED`. Follower execution and live money remain unauthorized.

Next source task: build and persist real management roll/covered-call candidate arrays before the management decision timestamp is frozen, then prove the canonical policy and Paper-plan paths with broker submission disabled. Runtime release remains blocked on a writable database, broker reconciliation and a real no-submit test.
